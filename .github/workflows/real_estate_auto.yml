name: Real Estate Cloud Automated Pipeline

on:
  schedule:
    - cron: '0 0 1 * *'  # 매월 1일 자정(UTC) 자동 실행
  workflow_dispatch:     # GitHub 웹 UI에서 수동 즉시 실행 지원

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas matplotlib

      - name: Run Automation Engine
        env:
          BOK_ECOS_KEY: ${{ secrets.BOK_ECOS_KEY }}
          DATA_PORTAL_KEY: ${{ secrets.DATA_PORTAL_KEY }}
        run: |
          if [ -f "real_estate_perfect_auto.py" ]; then
            python real_estate_perfect_auto.py
          elif [ -f "real-estate-news/real_estate_perfect_auto.py" ]; then
            python real-estate-news/real_estate_perfect_auto.py
          else
            python $(find . -name "real_estate_perfect_auto.py" | head -n 1)
          fi

      - name: Upload Generated Chart Artifact
        uses: actions/upload-artifact@v4
        with:
          name: market-analysis-chart
          path: |
            cloud_automated_market_analysis.png
            **/cloud_automated_market_analysis.png
            */**/cloud_automated_market_analysis.png